global_transactions:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schema_directory: '.'
      schemas_and_paths:
        main: 'global_transactions.db'