name: 'global_transactions'
version: '1.0.0'
config-version: 2

profile: 'global_transactions'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  global_transactions:
    staging:
      +materialized: table
    intermediate:
      +materialized: table
    marts:
      +materialized: table

tests:
  +severity: warn
